apiVersion: v1
kind: Service
metadata:
  name: podmonitor-kube-prometheus-stack-tritonmetrics
spec:
  type: NodePort
  ports:
    - name: web
      nodePort: 30900
      port: 9090
      protocol: TCP
      targetPort: web
  selector:
    podmonitor: kube-prometheus-stack-tritonmetrics
